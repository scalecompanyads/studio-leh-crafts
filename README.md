# studio-leh-crafts

## Meta CAPI

Configure `META_CONVERSIONS_ACCESS_TOKEN` no ambiente para que a rota `/grupo` carregue o Pixel `978455578134970`, envie o evento `Lead` pelo navegador e também replique a conversão pela CAPI antes do redirect para o grupo do WhatsApp.
